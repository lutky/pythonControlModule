# This is a sample secondary script that updates a shared variable
# You can replace this with your own logic

import time

shared_variable = 500

while True:
    # Replace with code to update shared_variable in real-time
    #shared_variable += 1
    #time.sleep(1)
    print(me)
